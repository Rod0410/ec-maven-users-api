name: workflow Azurecr

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.3
      
      - name: Setup Java JDK
        uses: actions/setup-java@v5.2.0
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build with Maven
        run: mvn clean package -DskipTests

      - name: Login to ACR
        run: echo "${{ secrets.ACR_PASSWORD }}" | azure login -u "${{ secrets.ACR_LOGIN_SERVER }} -u "${{ secrets.ACR_USERNAME }}" --password-stdin

      - name: Set Image Name
        id: vars
        run: echo "IMAGE_NAME=${{ secrets.ACR_LOGIN_SERVER }}/$(basename ${{ github.repository}})" > "$GITHUB_ENV"
      
      - name: Build The Docker Image
        run: docker build -t $IMAGE_NAME:latest .

      - name: Push The Docker Image
        run: docker push $IMAGE_NAME:latest
